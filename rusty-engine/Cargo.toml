# Rusty Engine workspace — run from this folder: cargo check --workspace
[workspace]
resolver = "2"

members = [
    "pod-race",
    "racer-ferris",
]

[workspace.dependencies]
rusty_engine = "7.0.0"
rand          = "0.10.1"
