# Rusty Engine workspace — run from this folder: cargo check --workspace
# Both members share rusty_engine 5.2.1 and rand 0.8.5.
[workspace]
resolver = "2"

members = [
    "pod-race",
    "racer-ferris",
]

[workspace.dependencies]
rusty_engine = "5.2.1"
rand          = "0.8.5"
