[package]
name = "missile-commander"
version = "0.1.0"
edition = "2021"
description = "Missile Command tutorial"

[dependencies]
macroquad = { version = "0.4.14", default-features = false, features = [
    "audio",
] }
